<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VitaAlarm_movil" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/nunito</item>
    </style>

    <style name="YellowOutlinedButton">
        <item name="android:background">@drawable/rounded_border_button</item>
        <item name="android:textColor">@color/yellow_main</item>
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:foregroundTint">@color/yellow_main</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="Theme.VitaAlarm_movil" parent="Base.Theme.VitaAlarm_movil" />
</resources>